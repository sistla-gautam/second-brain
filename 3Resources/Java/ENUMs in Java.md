used to specify a set of values that can be used for the program and nothing else
## Declaring an ENUM
```java
enum enumName = {
VALUE1,
VALUE2,
VALUE3
}
```
- the values that are inside the enum need to be in all caps